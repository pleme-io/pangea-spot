# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

# Local development overrides (remove when pangea-* gems are published).
gem 'pangea-core', path: '../pangea-core'
gem 'pangea-aws',  path: '../pangea-aws'
